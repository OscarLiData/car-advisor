# =============================================================================
# Car Advisor — Variables d'environnement
# Copier ce fichier en .env et adapter les valeurs
# NE PAS committer .env (déjà dans .gitignore)
# =============================================================================

# -- Chemins des données --
DATA_RAW_PATH=data/raw/ademe-car-labelling.csv
DATA_PROCESSED_PATH=data/processed/cars_clean.csv

# -- API --
API_HOST=0.0.0.0
API_PORT=8000
API_TITLE=Car Advisor API
API_VERSION=0.1.0
LOG_LEVEL=INFO

# -- PostgreSQL (laisser vide pour mode CSV) --
# DATABASE_URL=postgresql://caradvisor:caradvisor@localhost:5432/caradvisor
DATABASE_URL=
